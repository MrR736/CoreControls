@echo off 
cd %core%\CoreUpdates
del /s /q "%core%\CoreControls"
xcopy /s /y "%root%\Update\CoreControls-main" "%core%\CoreControls"
timeout /t 3 /nobreak
if not exist "%root%\Update" (
  echo Folder does not exist.
  exit /b
)
rd /s /q "%root%\Update"
exit
